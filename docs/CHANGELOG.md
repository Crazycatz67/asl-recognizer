# Changelog — dated revision history

Newest first. This is the full, dated record of what changed and why, moved
here verbatim from `asl-letter-recognition-plan.md` on 2026-09-23 so the plan
doc can stay short. The plan doc keeps only current status, the session-resume
block, and the active backlog.

**Conventions (unchanged):** add one dated entry per change, at the top of the
relevant section below. Say what changed, *why* (the feedback or plan item it
answers), how it was verified, and any `sw.js` VERSION bump. Stage labels:
`B#` = backlog item (see `docs/original-plan-and-milestones.md`), `S#` = a stage
of the 2026-09-12 mobile-restructure/demo-hand stage plan, `M#` = a North-star
milestone. Stage 1–8 of the **2026-09-23 showcase-polish plan** are tracked in
the plan doc's "Showcase polish" section.

> **Cross-references in the verbatim entries below** ("see the Stage 8 section",
> "the Backlog section", "North star", "_Stage 9 → Skin tone_") point at sections
> that now live in `docs/research-and-future.md` (Stage 8, Stage 9) and
> `docs/original-plan-and-milestones.md` (status table, North star, Backlog,
> Tasks 1–7). "The plan's S7 section", "the plan's Mobile findings table" and
> `twinkly-floating-garden.md` refer to the separate 2026-09-12 stage-plan file,
> which is not in this repo.

---

## 2026-09-23

- **2026-09-23 (docs reorganization):** The plan doc was split. Revision history
  moved here (verbatim); the Stage 8/9 research and design sections moved to
  `docs/research-and-future.md` (verbatim); the original Stage 1–7 plan, the
  2026-09-03 status table, the North-star milestones, and the B1–B21 backlog
  tables moved to `docs/original-plan-and-milestones.md` (verbatim). The plan
  doc now carries a new "Showcase polish (2026-09-23 plan)" section as the
  active backlog. README rewritten as a project overview; `Bug Reports/checklist.md`
  gained items 17–28 from 2026-09-23 live testing; `docs/code-reorg-proposal.md`
  added (proposal only, not executed). No code changed.
- **2026-09-23 (showcase Stage 1, decoder, in progress, uncommitted):**
  `js/decode.js` gains an exact-safe pruning floor and an incremental
  prefix cache, so appending a letter reuses the beam state of the prefix
  instead of re-decoding from scratch. Measured on an offline bench: a cold
  40-letter decode went from 315 ms to 25 ms, and each appended letter costs
  about 1 ms. Output was identical to the old decoder on a 24-phrase bench.
  This is the fix for checklist item 17 (Spell mode freezes as the text grows).

## 2026-09-12 → 2026-09-15 (reconstructed from git log)

These commits never got a plan-doc revision-history entry. The one-line summaries
below are taken from their commit subjects and bodies. The commit message is the
full record (`git show <hash>`). Where the commit also has a verbatim entry
further down (S2d part 2, S2e, S3, S4a, S4b), it isn't repeated here.

- **2026-09-15 `41c43f0` (checklist #2, part D):** One-euro adaptive smoothing (new `js/onefilter.js`) replaces the fixed-alpha EMA in `smoothLandmarks`. This fixes the ghosting/lag half of item #2. The raw-fps half stays open. (Also closes backlog B18.) `sw.js` v68 → v69. selftest 174/174.
- **2026-09-15 `342f3cf`:** `.gitignore` update. The Stop hook's blanket `git add -A` had twice pulled unrelated local tooling into WIP checkpoints.
- **2026-09-15 `948352b` (S5):** You can now skip a letter mid-run in an A→Z/Review run instead of only being able to abandon the whole run. `sw.js` → v68.
- **2026-09-15 `62a606c` (S5):** Spell's 240-char cap now says so instead of silently dropping letters. There is a new regression test. selftest 174/174. `sw.js` → v67.
- **2026-09-15 `f4bbf60` (S5):** Debug telemetry behind `?debug`. The intro modal is now keyboard-accessible and scrollable. `sw.js` → v66.
- **2026-09-15 `5629637` (S5, instructional and trust layer):** Live reduced-motion and honest Spell copy. `sw.js` → v65.
- **2026-09-15 `f66a683` (checklist #7):** `js/posekin.js` interpolates each bone's true 3D direction (SLERP) instead of its 2D-projected angle. This fixes W/R/X/K/V's impossible demo-hand swings. selftest 173/173.
- **2026-09-13 `0757b22` (S2d part 1):** Kills the J/Z demo-hand's reverse retrace (hold → crossfade → restart).
- **2026-09-13 `b1ab432` (checklist #6):** Fixes the J/Z practice charge-tone audio buzz. `sound.charge()` is now fed an eased value.
- **2026-09-13 `2956385` (checklist #3):** Holds the overlay through a momentary detection miss (`OVERLAY_GRACE_FRAMES`).
- **2026-09-13 `d640d12`:** Adds `tools/testHarness.js`, which drives the real running app with synthetic hand landmarks instead of a webcam.
- **2026-09-13 `acfcdbb`:** Adds the `asl-bugwatch` skill and the `Bug Reports/checklist.md` tracker.
- **2026-09-13 `2f75e95`:** Fixes the live guide showing yellow correction markers on shapes already scored correct. Adds the shared `reference.matchTolerance()`.
- **2026-09-13 `195ac35`:** Adds the project-scoped skills (`asl-verify`, `asl-resume`) and the WIP-checkpoint Stop hook.
- **2026-09-13 `da942e2`:** B14/B15 QA follow-up: a "Test blind" mode for bounded practice runs.
- **2026-09-13 `53f6be5`:** Fixes the live-guide finger-name label rendering backwards under the mirrored stage.
- **2026-09-12 `8b5d88c` (S2c):** Depth cues for the demo hand, using the z data the letter centroids already had. Rendering only.
- **2026-09-12 `15c60d4` (S2b integration):** Wires `posekin.js` into the demo-hand player. A curling finger now sweeps an arc, and bone lengths are pinned.
- **2026-09-12 `2356d5d` (S2b):** New `js/posekin.js`, bone-length-preserving interpolation over the hand's kinematic tree.
- **2026-09-12 `08787c7` (S2a):** Kills the per-frame bounding-box refit in the demo-hand animation. It made the hand swell, shrink and drift.
- **2026-09-12 `c9e3419` (S1):** Mobile triage. Fixes M1–M6, M9 and M11, plus a grid-blowout regression found in verification.
- **2026-09-12 `fbe45cd` (S0):** CI guardrails for the mobile restructure and the demo-hand plan (four additions to `tools/ci-check.mjs`).

## 2026-09-03 → 2026-09-13 (verbatim from the plan doc)

- **2026-09-13 (S4b — js/sheet.js + the reference panel as a bottom sheet):** New shared `js/sheet.js`: `createSheet(el, opts)` — `open()`/`close()`/`toggle()`/`isOpen()`, non-modal by default (just toggles an `openClass`) or `modal:true` (focus trap, `inert`s every OTHER top-level child of `inertRoot` — found via walking up from `el` to whichever direct child contains it, so it works no matter how deeply `el` is nested — Escape and backdrop-click close, focus moves in on open and back to the trigger on close). Built once per this doc's own instruction ("that is also S6's dialog fix, so build them as the same thing") — S4b only exercises the non-modal path; modal mode is unit-tested now (3 new `selftest.js` checks incl. the nested-container case) but awaits real UI wiring in S4c/S4d/S6. **Reference panel** (`index.html`/`css/style.css`): on mobile/portrait it's now `position:absolute` inside `.workspace` (given `position:relative` as the containing block) instead of a flex sibling that shared height with the camera — `.workspace` now has only `.viewport` as a flex participant, so the camera gets the full stage. Collapsed by default to a peek strip (handle + meter + a 1-line-clamped hint) via a new `#refHandle` button wired to `refSheet.toggle()`; expands to the full card (max-height 44svh) on open, which happens automatically whenever a new letter's reference appears (`setTarget`, the Challenge-HUD clear path, and un-checking "Test blind" — the three places `refPanel.hidden` already turns false) so the default experience is unchanged from before S4b, collapsing is a new opt-in for more camera room. Landscape/desktop (existing `min-width:700px` media query) overrides all of this back to `position:static`, always-expanded, handle hidden — the same side-column experience as before S4b, unchanged. **A real measurement caught a plan-doc estimate that didn't hold**: the plan's own "~72px" collapsed-height figure assumed a short hint; a realistic correction message ("Straighten your middle finger — extend it out fully") measured 136px uncollapsed, so the hint is now 1-line-clamped (matching the existing `.ref-desc` pattern) and the collapsed cap raised to a measured-not-guessed 7.6rem (~122px) — confirmed via `scrollHeight`/`clientHeight` equality (no overflow, no scrollbar) after the fix, not just eyeballed. Verified: `node tools/ci-check.mjs` all pass (sw.js CORE list + VERSION updated for the new file, → v63); `tools/selftest.html` 173/173 (170 + 3 new sheet.js checks); live in an injected iframe — expand/collapse toggling confirmed correct after accounting for CSS-transition throttling in this environment (same class of limitation as the already-documented rAF throttling — forcing a reflow with the transition disabled proved the underlying values were always correct, it was purely a test-timing artifact); zero overflow across all 4 modes at 375×667; the landscape side-column view confirmed pixel-identical in behavior to pre-S4b.
- **2026-09-13 (S4a — shell grid, first of S4's sub-stages):** S4 has no pre-defined sub-stages in this doc the way S2 did (S2a-e) — given the size ("Highest layout-regression surface" per this doc's own Risk section) and that final acceptance needs a real device this environment can't provide, the work was split, with the owner's confirmation, into: **4a shell grid** (this entry) → 4b `js/sheet.js` + reference panel as a bottom sheet → 4c letter picker as a bottom sheet → 4d per-mode thumb dock → 4e breakpoint/unit cleanup (height query, `svh`, 16px inputs, deleting the M12 hardcoded chrome constants). **This step:** `body` changed from `display:flex; flex-direction:column` to `display:grid; grid-template-rows: auto minmax(0,1fr) auto` (topbar / stage / dock) — the exact structural fix this doc's Shell section calls for, since a grid track can't be squeezed to 0 by an oversized sibling the way a `flex:1` item can be by `flex:none` siblings. `#learnRow` and `#workspace` are now wrapped in a new `.app-stage` (`index.html`) so they occupy the grid's single "stage" row together, `.controls` occupies the "dock" row (its real per-mode content is S4d's job — for now it's the same camera-only toolbar as before, just correctly slotted). Added `env(safe-area-inset-top)` to `.topbar` and `env(safe-area-inset-bottom)` to `.controls`. Deliberately did NOT touch: the M12 hardcoded `calc(100dvh - 150px)`/`130px` landscape camera sizing (still works identically under the grid — S4e's job to replace with a real fix), the reference panel / letter picker / Spell folds (still inline panels — S4b/c/d), and the topbar's now-inert `flex:none` (harmless on a grid item, cosmetic cleanup only). Verified: `node tools/ci-check.mjs` all pass (id cross-check confirms the wrap didn't orphan anything); `tools/selftest.html` 170/170 unchanged (pure CSS/HTML, touches no module logic, per this doc's own Risk note); live-tested via an injected same-origin iframe (`resize_window` doesn't affect this environment's actual rendered viewport — confirmed via `window.innerWidth`, so an iframe was used instead, since same-origin iframes get their own real CSS viewport for width/height media queries) at 6 sizes (360×640, 375×667, 390×844, 667×375, 812×375, 320×480, 1024×768) with `body.scrollHeight === body.clientHeight` (zero page scroll) confirmed at every one; visually checked Practice/Challenge/Spell/Read all render correctly at 375×667 and the existing landscape two-column layout (camera + reference panel) still works unchanged at 850×500. `sw.js` VERSION → v62. **Still needs, per this doc's own Risk section — cannot be done here:** real-device acceptance (iOS Safari + Android Chrome, portrait and landscape, URL bar shown and hidden, one-handed while actually signing).
- **2026-09-13 (S3 — Read mode as the real receptive tool):** All five plan bullets shipped. **Transport** (`js/reference.js`): `pause()`/`resume()`/`seek(ms)`/`isPaused()`/`elapsedMs()`/`wordInfo()` added to the player; `buildWordSpans` now also returns `letterStarts` (the elapsed-ms each letter's own hold begins) so the UI can step to an exact letter. **Scope boundary, explicit** (checked against real usage the same way S2e's J/Z boundary was, not assumed): transport only applies to a word with no J/Z, played as one continuous `setWord` run the controls can act on — a word containing a motion letter has no single clean timeline to scrub (a stroke is a self-contained animation, not a bone-space pose), so `js/main.js`'s `playWord` falls back to Replay-only for those, confirmed live by brute-forcing through Read-mode words until "pizza" (contains Z) correctly hid the pause/step/scrub row while every plain word showed it. UI: pause/step‹›/a scrub `<input type=range>` with per-letter `<datalist>` tick marks in `.rd-playbar`, live-synced to the player's own clock via a small rAF loop in `main.js` (not the player's — the player has no UI concerns). **Near-miss feedback** (`js/reader.js`): `check()` now returns `{ok, diff, confusables}` instead of a bare boolean — `diff` is a positional letter-by-letter comparison, `confusables` cross-references `data/confusion.json` (already loaded for the fluid decoder) to say e.g. "N and M are easy to mix up," with a "show me those two side by side" button rendering both letters' reference photo + how-to text inline. Updated the pre-existing `selftest.js` reader tests (`=== true`/`=== false`) to read `.ok` instead — an object is always truthy, so leaving them unchanged would have silently broken the wrong-guess path. **Found and fixed a real race condition while wiring this in:** `main.js` loads the word bank (small, fast) and the decoder's word list + confusion matrix (words25k.txt is much bigger, especially over a real network) as two independent parallel fetches; `reader = createReader(bank, {confusion: loadedConfusion})` very often runs before `loadedConfusion` is populated, and since `createReader` originally captured it in a `const` closure, the reader would silently never get confusion data on a real deployment even though it looked fine on a fast local server (confirmed both orderings are live races via `performance.getEntriesByType('resource')` timing). Fixed with `reader.setConfusion(map)` (the closure var is now `let`), called from the decoder loader once confusion data actually arrives — covered by a new regression test, not just noticed and patched. **Wrong answers persist until dismissed:** replaced the `setTimeout(nextReadWord, 1600)` on a wrong guess with a "Next word →" button (`#rdNext`) the learner clicks when ready; correct answers keep auto-advancing (the plan's complaint was specifically about wrong answers not giving enough time to read the diff/confusable note). **Mute button relocated** from the camera-only `.controls` toolbar (`hidden` outside Practice/Challenge/Spell) into the always-visible `.topbar`, next to the contrast toggle — Read mode's Check/Reveal sounds had no mute control at all before this. **Surfaced the three silent failures**: `practice-words.json` (word bank + spell-drill starter pool), `curriculum.json` (Course lessons), and the decoder's `words25k.txt`+`confusion.json` — each now a retriable `function load*()` instead of a fire-and-forget fetch with `.catch(() => {})`, with a visible "⚠ couldn't load — Retry" banner (`.load-error`) that re-checks on mode entry too (not just at fetch time), so a failure that happened while the user was in a different mode still surfaces once they switch into Read/Spell. Verified: `node tools/ci-check.mjs` all pass; `tools/selftest.html` 170/170 (new: transport pause/resume/seek/wordInfo, the diff/confusables shape, `setConfusion`); live in-browser — pause genuinely freezes (zero drift confirmed over a 300ms wait via a single-round-trip script, not screenshot timing which is unreliable in this environment per [[asl-verify-limits]]), step forward/back land exactly on `holdMs`-spaced letter boundaries, scrubbing works, the near-miss diff+confusable+compare-panel renders correctly end-to-end for a real wrong guess, the mute button toggles and dims in the topbar, and the load-error banner's styling renders correctly. `sw.js` VERSION → v61.
- **2026-09-13 (S2e — coarticulation, closes out S2):** **Word-level demo-hand animation rebuilt to chain letters directly in bone space instead of detouring through neutral for every letter.** The old `playWord` (`js/main.js`) called `setTarget` once per letter on a `setTimeout` ladder; since `setTarget` always resets to a fresh NEUTRAL_HAND→letter cycle, any word faster than the ~2.9s single-letter cycle looked like a neutral-hand flicker between letters, exactly the plan's complaint. New `createCanonicalPlayer.setWord(items, opts)` (`js/reference.js`) takes a run of *static* letters (`items = [{letter, vec}]`) and chains them with: **no neutral detour mid-word** (only the first letter eases in from neutral, and that entry now shares the first letter's own time budget rather than adding extra time — see `buildWordSpans`'s comment on why, a real timing bug caught before it shipped); **distance-scaled transitions** (`wordTransDur` = `posekin.js`'s new `angleDistance` bone-angle RMS, clamped to the plan's [90,340]ms); **anticipatory blending** (`WORD_BLEND_BETA=0.15` — a hold sits at a slight bone-space blend toward the next letter, via the same interpolator the transition itself uses, so the join is seamless); **overshoot arrival dynamics** (new `strokekin.js` `easeOutBack`, tuned to ~6% peak overshoot settling exactly at 1, fed into `posekin.js`'s `makeInterpolator` — which had its `t` clamp deliberately removed so the overshoot survives instead of being flattened); **a doubled-letter wrist bounce** (`translatePose`, a small rigid nudge-and-return over 140ms) instead of a bone-space blend toward an identical pose, which would otherwise render a doubled letter (the LL in HELLO) indistinguishable from a single one. **Scope boundary, explicit:** J/Z have no single target shape to chain through in bone space, so `main.js`'s `playWord` splits a word into runs at each J/Z, calling `setWord` per static run and keeping today's existing `setMotion` timeline for the motion letters themselves — checked against the real word lists first (26/440 words contain J/Z, several mid-word, including the doubled "zz" in "jazz") rather than assumed rare; confirmed via the new run-splitter that every real case (a leading/trailing/mid-word motion letter, and back-to-back motion letters) produces a sensible call sequence with no exceptions. Speed (`#rdSpeed`) now sets `holdMs` (the per-letter time *budget*, unchanged calling convention) rather than directly controlling a cycle that ran at a fixed duration regardless of speed — transition length is derived from the shapes, not from speed, per the plan. Verified: `node tools/ci-check.mjs` all pass, including a new regression test (not just a smoke test) asserting the exact timing contract a 3-letter run's total is exactly `3×holdMs`, spans are gapless, sampling past the end freezes on the last letter, and a doubled letter's bounce returns exactly to its own pose; `tools/selftest.html` 167/167; live in-browser against the real dataset centroids for "HELLO" — sampled 11 points across the full timeline and confirmed a continuous, sensible progression (H→E→L→[bounce]→L→O) with the double-L bounce visibly nudging position while preserving the handshape, and separately confirmed the exact word-splitting `main.js` uses produces no exceptions for DOZEN/PIZZA/JAM/ZONE/BRAZIL/JAZZ. `sw.js` VERSION → v60 (no new precached file this time). **S2 (demo-hand rework) is now fully done — S2a through S2e all shipped.** Next per the plan: S3 (Read mode as receptive tool).
- **2026-09-13 (S2d part 2):** **J's demo-hand motion rebuilt as forward kinematics; Z gets a wrist cock, arrowhead, and numbered waypoints** — the two items S2d part 1 left open. New pure module `js/strokekin.js` (`rotate2D`, `catmullRom2D`, `delayedEase`, `rigidPoseAt`, `bump`, `arcFractions` — no DOM, unit-tested in `tools/ci-check.mjs`'s check #11). **J** (`js/reference.js`): the rigid "I" handshape now rotates around its own gliding wrist (`jPoseAt` — theta 0° → −95° supination, delayed until 50% through the clip so the downstroke reads as a straight line, wrist gliding along a 4-point Catmull-Rom arc) instead of a hand-plotted fingertip polyline; the dashed guide + bright progress trail are *sampled* from that same motion (`sampleTrail`), so the "hook falls out of the rotation" per the plan rather than being authored separately. **Z** kept its existing translation along `STROKE.Z` and gained: a small ±8° wrist cock at each of the path's two direction-change corners (`zCockRad`, a raised-cosine bump at each corner's arc-length fraction), a direction arrowhead at the current tip, and numbered waypoint markers (1–4) at the path's corners. `paintStroke`/`paintStrokeCrossfade` now dispatch on a new `stroke.kind` (`"rotate"` for J, `"translate"` for Z) to two focused paint functions instead of one function assuming a translating fingertip. `motion.js`'s `STROKE.J` (used by the live on-camera swoosh guide in `overlay.js` and the real-time detector's thresholds) is untouched — this change is scoped to the canonical demo player only. `sw.js` CORE list + VERSION (→v59) updated for the new file. Verified: `node tools/ci-check.mjs` all pass (new strokekin invariants: rotate2D length/composition, catmullRom2D exact control-point pass-through, rigidPoseAt identity case, bump/arcFractions range); `tools/selftest.html` 166/166; live in-browser (Practice panel, both letters) — J traces a clean vertical-then-hook shape with the bright trail exactly overlaying the dashed guide, the hold→crossfade→restart blend works with no reverse retrace (carried over correctly from part 1); Z shows all 4 numbered waypoints and the dashed/crossfade phases correctly. The 8° cock and arrowhead are real (unit-tested, code-reviewed) but too subtle to visually confirm at the small demo-panel canvas size in this environment. **Pre-existing, unrelated finding, not caused by this change:** the tap-to-enlarge overlay's canvas (`demoZoomCanvas`) renders blank for J/Z specifically (confirmed via `git stash` — same behavior on the pre-change code); static letters enlarge fine. Left open, not investigated further — out of scope for S2d.
- **2026-09-11 (audit pass):** **Full codebase audit — bug hunt + adversarial testing, per user request** ("test each feature... limit test... look for ways to break the site... store it down so we can work on it next"). Method: re-ran `selftest.html` for a baseline (164/164), mapped every `js/*.js` module by size/exports (main.js is the 1967-line monolith — every mode's state, the per-frame camera loop, and all UI wiring live in one file with no per-mode isolation, which is exactly the shape that produces cross-mode bugs like the one found below), then did targeted static reads plus live adversarial testing in the browser (rapid mode-switching, corrupted `localStorage`, Read-mode Check/Reveal spam + huge/empty input, Spell-mode toggles and the A–Z reference grid with no camera, keyboard-only nav incl. the new `refPhotoBtn`, and a simulated `getUserMedia` permission-denial).
  - **Found + fixed a real bug:** `pendingChallengeStart` (a module-level flag `startChallenge()` sets when Start is clicked but the camera isn't up yet) was never cleared on camera failure or on leaving Challenge mode — only on success. Sequence: user clicks Start in Challenge, camera permission is denied (or any `start()` failure) → flag stays `true` forever → user switches to Practice (or Spell) and successfully turns the camera on there → `setState`'s pending-challenge check fires unconditionally on the next "searching"/"tracking" transition and calls `startChallenge()`, which sets `scoreBadge.hidden = false`, `chCard.hidden = true`, etc. and starts the actual game timer — **while the visible mode is Practice**. Confirmed via code (not live-reproducible here since a real successful camera stream isn't obtainable in this automation browser) that nothing CSS-side gates those HUD elements by mode (`grep` for `scoreBadge`/`timeBar` in `style.css` returns nothing — visibility is 100% JS-`hidden`-attribute-driven), so this would visibly overlay Challenge's score badge/timer on top of Practice or Spell mode. **Fix (`js/main.js`):** clear `pendingChallengeStart` in `setMode()` when switching away from Challenge, and again in `setState()`'s `!live` (error) branch; also added a `mode === "challenge"` guard to the auto-trigger itself as defense in depth. While in there, fixed a related cosmetic bug: `startChallenge()`'s "Starting camera…" message was immediately overwritten by `setState("requesting")`'s generic challenge-card-reset branch, which ran synchronously right after and had no reason not to fire — so the message never actually appeared. Fixed by adding a `!pendingChallengeStart` guard to that branch. selftest still 164/164 after the fix.
  - **Everything else held up.** No other bugs found despite deliberately hostile input: corrupted/garbage `localStorage` (invalid JSON, bogus enum values) falls back to sane defaults on reload, no crash; rapid mode-switch spamming produces no console errors; Read mode's Check/Reveal are properly guarded against double-fire (`rdInput.disabled` checked in both the form-submit and Reveal handlers) even under rapid clicking and a 250-char pasted answer; Spell mode's toggles, drill, Copy/Paste, and the tap-to-enlarge A–Z reference grid all work with the camera off and share the `openDemoZoom()` overlay refactor from earlier today cleanly (verified canvas/img visibility toggles correctly and resets on close); keyboard access works throughout (arrow-key letter stepping, direct a–z jump, Escape closes overlays, and the new `refPhotoBtn` is a real focusable button that activates on Enter); a simulated camera-permission-denial is caught and shown as a friendly "Camera blocked… Try again" state, not a crash.
  - **Not testable in this environment (needs the deployed site or a real device):** the actual live webcam loop (as already documented), and the PWA/offline path — `index.html`'s service-worker registration explicitly skips `localhost` (`!/^(localhost|127\.|\[::1\])/.test(location.hostname)`), so it never even registers during local dev; only the deployed site can confirm it, and only after `VERSION` in `sw.js` (still `v42`) is bumped past this session's HTML/CSS/JS edits.
  - **Minor pre-existing gap noted, not fixed (low severity, out of scope for this pass):** the `#demoZoom` enlarge overlay (both the canvas and the photo variant) has no focus trap — Tab can still reach controls behind it while it's open. Applies equally to the Practice reference photo, the Spell-mode A–Z grid, and Read mode's nothing-there-today equivalent. Worth folding into a real accessibility pass rather than a one-off fix.
  - **Optimization/architecture observation, not acted on:** `main.js` at ~2000 lines holds every mode's state as module-level `let`s with no shared "leaving a mode" cleanup contract — each mode's teardown is hand-written ad hoc inside `setMode()` (see the Spell block vs. the one-line Challenge/Read handling), which is exactly how the bug above slipped through. If another mode gets added, the same class of bug (a flag armed in one mode, consumed unconditionally after a state transition that can happen in any mode) is the thing most likely to break next. Not restructuring this now — flagging it as the thing to watch, and a candidate refactor (a small per-mode `{enter, leave}` table) if another cross-mode bug like this shows up.
- **2026-09-11:** **Spell-mode over-triggering, reference-photo sizing, and onboarding pass** (from live user + beta-tester feedback: sloppy fast transitions between letters were getting locked in as "correct"; the reference photo was too small to read and not tappable; the privacy/consent popup got clicked through in under a second without anyone reading it or learning what each mode does).
  - **`js/transition.js` retuned:** `SETTLE_MS` 90→115, `minConf` 0.5→0.6, `stillThr` 0.30→0.27. Validated with a throwaway Node sweep (`tools/replay-lab.html`'s pipeline reimplemented headless) against `data/fs_sequences.json`: tightening consistently raised precision (fewer spurious letter-locks) at a moderate recall cost. **Caveat carried over from [[asl-kaggle-domain-gap]]:** that dataset's landmarks are a domain mismatch for our classifier, so absolute accuracy numbers from the sweep aren't trustworthy — only the *relative* direction (tighter settle criteria → fewer false locks) is. First tried `SETTLE_MS=140`, which broke the `selftest.html` "C-A-T settles to three letters" fixture (a 7-frame/~230ms held letter no longer settled in time once the post-move settle-start delay is counted) — backed off to 115ms, which passes. **Needs a live webcam check** to confirm it actually feels less trigger-happy without feeling sticky.
  - **Reference photo (`#refImg`) was static, unclickable, and fixed at 72px** — too small to read finger detail, per direct feedback. Wrapped it in a new `#refPhotoBtn` button and wired it into the existing demo-zoom overlay (`#demoZoom`, previously canvas-only) so tapping it shows a full-size version, same as the animated demo already did. Also restructured `.reference`'s mobile/portrait grid from a cramped 3-column strip (`photo canvas match` all in one row) to the same 2-column stacked layout landscape already used (`photo canvas` / `match match`) — the old portrait-only strip was the direct cause of "everything feels compressed." Both photo and demo canvas now flex up to 160px (was a hard 72px in portrait, 138px max in landscape).
  - **Onboarding (`#intro`) rewritten:** the privacy line was buried inside step 1's prose and the popup was instantly dismissible, so testers blew past it without reading it or learning what Spell/Read/Challenge even do. Now: privacy gets its own highlighted callout above the numbered list, the three steps cover all four modes (was missing Spell and Read entirely), and the "Got it" button is disabled with a visible countdown for ~2.5s before it's clickable — forces at least a skim without being a hard wall.
  - Self-test still **164/164**. Not camera-verified this session (see Can't-verify-in-session above); the reference-photo tap-to-zoom and onboarding countdown were checked visually in-browser.
  - **Explicitly deferred, not attempted:** the demo-hand-animation "flips weirdly" complaint (see the new backlog bullet above — root cause identified, fix is bigger than this session's scope) and a full mobile CSS audit beyond the reference panel (other panels — Spell, Challenge, Read — weren't touched and may have similar compression issues worth a follow-up pass).
- **2026-09-04:** **Demo hand is now a solid "mannequin hand", not a stick figure** (user: thin lines + dots are hard to read). New `drawHandShape(ctx, pts, opts)` in `js/skeleton.js` — same 21 points, rendered as a filled palm blob + fat rounded fingers/thumb (each digit outlined separately so neighbours show a seam) + knuckle creases + little nails, on a soft grey vertical gradient. `createCanonicalPlayer` (`reference.js`) and `drawCanonical` now draw the solid hand only — **no skeleton overlay** — so it drives the Practice reference panel, the enlarged demo, and Read mode's animated speller hand. Checked the A–Z centroids: fists (A/E) read as fists, and B/C/L/O/V/W/Y are all clearly the sign; the live-camera correction guide keeps the thin skeleton (there you need the joints). Also: **SW registration now skips `localhost`** (was serving stale JS every dev reload via stale-while-revalidate — cost real time this session) and `sw.js` `VERSION` → `v42`. Self-test 163→**164** (+1 `drawHandShape` renders opaque pixels, no throw on junk).
- **2026-09-04:** **B8 shipped — installable PWA + offline.** `manifest.webmanifest` (name/short_name/description, `start_url` + `scope` `./` so it works at `/` in dev and `/asl-recognizer/` on Pages, standalone display, `#0f172a` theme) + `icons/` (192 / 512 / maskable-512 / apple-touch — a flat white raised-hand mark on the app's cyan gradient, generated by `tools/make_icons.py` via Pillow). `sw.js`: precache the shell on install (`CORE` = html/css/all-js/manifest/icons, atomic; `EXTRA` = dataset.json + word list + curriculum/practice/confusion + the 26 reference jpgs, best-effort) → **stale-while-revalidate** for same-origin (offline-capable *and* picks up new bytes on the next load), **cache-first** for the immutable MediaPipe CDN assets (jsdelivr wasm + the `hand_landmarker.task` model, grabbed on first camera use), navigations fall back to the *cached page itself* then `index.html`. `VERSION` const clears old caches on `activate` (**bump per deploy**). Registration is a 3-line inline `<script>` in `index.html` + `about.html`; both get `<link rel=manifest>` + `theme-color` + `apple-touch-icon`. `.webmanifest` MIME added to `serve.ps1`. **Verified: stopped the dev server, reloaded — the whole app (HTML + all modules executing) and the about page both loaded from the SW cache.** Self-test 163/163 (no JS-module change). **This clears the camera-free queue** — everything left needs the webcam session or people.
- **2026-09-04:** **Stage 9 sharpened — dual-engine architecture + the skin-tone / data-source analysis.** User's call: keep the k-NN as **Engine A** (Practice/Challenge/Read/Spell/drill — instant, transparent, no download) and add a landmark **sequence model as Engine B** powering an opt-in **Conversation mode**; both feed the same `decode.js` + `speechSynthesis` tail and sit on the same MediaPipe front-end; Engine B lazy-loads (ONNX Runtime + model) and falls back to today's fluid path on failure; optional background cross-check between the two. Added a **"Skin tone — our data vs. Google's approach"** subsection: both pipelines are landmark-based so the *classifier* is skin-blind by construction (true for Engine A and B); the disparity risk is the shared upstream **pixel→landmark detector**, which the Kaggle data does **not** change (it's already landmarks). Google's data helps via *breadth* (100+ Deaf signers, real capture conditions) not a targeted fix, and ships no skin-tone labels; the transferable part of "Google's approach" is the **Monk Skin Tone (MST) 10-point scale** as an evaluation yardstick. The real fix = an MST-stratified measurement panel (detection rate + landmark jitter + letter accuracy per band); wired into the Stage 5 / Task 6 skin-tone check. No code.
- **2026-09-04:** **About page shipped (M5).** New self-contained `about.html` (own inline CSS, reuses the app's dark palette + radial-gradient ground; scrollable document, unlike the locked single-screen app). Sections: what it is (student project, on-device) · **what it does** (green card — the 4 modes + J/Z + fluid/speak beta) · **what it does *not* do** (amber card — not ASL, not native speed, no digits yet, names don't auto-correct, *mostly tested by one person*, fluid mode unproven) · how it works (MediaPipe → normalize → kNN + heads → stabilise; ~97% held-out framed explicitly as a ceiling) · privacy (nothing uploaded, localStorage only) · where it's headed (sequence model, context lexicons, do the next part *with* Deaf signers) · credits & data (MediaPipe, grassknoted dataset, Norvig list, PopSign) · open-source repo link. "About" link added to the topbar (`.topbar-link`, right-aligned, wraps on narrow). Writing it doubled as the honest-inventory pass. No JS, no selftest delta (163/163). **One TODO for the user:** add a name / contact line in the footer if they want it public (HTML comment marks the spot).
- **2026-09-04:** **B4 shipped — word content ×2.5 + Spell-mode practice targets (camera-free build).** (1) **`data/practice-words.json` expanded** ~150→~370 words: grew short/common/names/places/numbers, added **food / animals / verbs** categories, roughly familiarity-ordered. Feeds Read mode, the drill, and `decode.js` practice. (2) **Spell-mode word drill** — a "Practice a word" toggle (+ source select: **from my lesson** = the active Course tier, or **common words** = short+common starter pool). New pure module `js/spelldrill.js` (`createSpellDrill`: `next()` / `match(attempt)→{n,ok,bad}` / `submit()` / `skip()` / `setWords()` / score·streak·done). A prompt row shows the target spaced out; as you spell, the matched prefix goes **green**, a diverged letter **red**; an exact match → green flash + chime + haptic, then the next word. Reads the attempt straight out of `speller.pending` (works in both plain and fluid mode); `drillHitAt` debounce guards the advance. State persists (`asl-pref-drill` + `asl-pref-drill-src`). This also **wires the productive side of the curriculum** the plan flagged as owed — Spell mode can now drill the exact tier you're learning in Read mode. Self-test 155→**163/163** (+8 spelldrill: prefix match, divergence flag, streak scoring, `setWords` swap, no-repeat, empty-pool safety). Verified in-browser: toggle + row + spaced prompt + Skip + both sources (starter → "river", course → "boy" from the Anchors tier) + persistence; the letter-colouring + auto-advance run only in the camera loop, so that half is proven by the module's unit tests + a simulated paint, not a live spell. **M2 core is done** (B1 ✓ B2 ✓ B4 ✓ B5 v1 ✓).
- **2026-09-04:** **B2 shipped — a real curriculum (Read-mode Course, camera-free).** Read mode gains a **Free play / Course** toggle. Course walks 26 letters in a teaching order grouped by handshape difficulty — **Anchors** `A B C L O Y` → **One/two fingers** `D F I K U V W` → **Pointing & flat** `E G H P Q R` → **Fists & look-alikes** `M N S T X` (the confusable cluster) → **Motion letters** `J Z` — each tier's practice words use only letters taught so far (`data/curriculum.json`, 12 words/tier, hand-curated). New pure module `js/curriculum.js` (`createCourse(json, saved)`: `tier`/`words()`/`record(ok)`/`select(i)`/`view()`/`state()`/`taughtLetters()`/`complete`). **Progress gating:** a tier unlocks after `unlockThreshold` (10) correct answers *while its predecessor is active*; progress is per-tier and never decreases; wrong/revealed answers don't advance it. UI: a lesson **path** of tier pills (active / done ✓ / locked 🔒, click an unlocked one to jump), a lesson header (name · spaced letters), blurb, and a fill **progress bar**; a promotion shows "✓ correct — 🔓 new lesson: …". State persists (`asl-pref-course` + `asl-pref-read-style`); `reader.next(list)` gained an optional caller-supplied word list so Course drives selection while Free play keeps its category chips. Verified in-browser: toggle, path/locks, header, bar, tier-constrained words, correct-answer increment + persistence all work; no h-scroll at 280 px. Self-test 145→**155/155** (+1 `reader.next(list)`, +9 curriculum: unlock, gating, `select`, `state()` round-trip, frontier-only promotion, `taughtLetters`, completion). The letter-*shaping* half of a lesson still needs the camera (Practice mode) — the course spine, ordering, gating and progress are all camera-free and done. **M2 nearly complete** (B1 ✓, B2 ✓, B5 v1 ✓; B4 word-content expansion + Spell wiring remains).
- **2026-09-04:** **B5 — accessibility pass (v1).** One consistent high-visibility keyboard focus ring everywhere (was: only the read input); `@media (prefers-contrast: more)` strengthens the palette; Escape closes overlays; dynamic aria on the mute button + `aria-pressed` on the mode buttons; `role="tablist"` → `role="group"` + labels on the toggles; `role="group"` + item labels on the category chips and alphabet chart; `prefers-reduced-motion` extended to the new Spell/gesture surfaces. Self-test 145/145. Still open for a full B5: an in-app high-contrast *toggle* (not just the OS query), a screen-reader walkthrough, captions audit on every transient sound.
- **2026-09-04:** **B1 shipped — Read mode (receptive practice).** A 4th mode, no camera: the animated canonical hand (`createCanonicalPlayer` on `#rdCanvas`) spells a word letter-by-letter at a chosen speed, you type what you saw. `js/reader.js` (pure: `next`/`check`/`reveal`/category toggles, streak-scaled score) + `data/practice-words.json` (5 categories, ~150 words — also B4's content + decoder practice material). Length-dot hint, green/red feedback, auto-advance, mode persists, camera viewport hidden. Mode toggle wraps + shrinks for the 4th button (no h-scroll at 280 px). Self-test 140→**145/145**. **This is the "harder half" of the skill the app didn't train — the biggest single feature for the RIT showcase.** Next M2 items: B2 curriculum, then B5 accessibility.
- **2026-09-04:** **Fluid-mode verification pass — found + fixed 3 wiring bugs.** Can't run the webcam, so built a `selftest` "fluid pipeline" block that round-trips `transition → speller → decode` on synthetic *clean* sequences (a good-webcam simulation). It caught: (1) **mid-word premature commit** — `speller.add()` didn't reset `offSince`, so in fluid mode the word-break timer kept running while letters arrived via `addLetter()` → "HELLO" committed as "HEL L O"; (2) **auto-speak was dead in fluid mode** (`fluidLastLetterAt` only set on `res.event === "letter"`, which fluid never emits); (3) **Clear didn't reset the transition matcher**. All fixed; selftest 136→**140/140**; non-fluid path unchanged. The `webcam → HandLandmarker → classifier → transition` front half still needs a live test (matched domain, user-confirmed elsewhere); the `transition → speller → decode → display → speak` back half is now integration-covered.
- **2026-09-04:** **Diagnosis: the Kaggle landmarks are a domain mismatch — B3-as-kNN-retrain is off the table.** The DTW harvest tool (`tools/harvest-kaggle.html`) works but adding 1364 samples moved replay 0%→0%. Root cause: the competition's MediaPipe **Holistic** hand landmarks aren't interchangeable with **Hands** (`HandLandmarker`) landmarks — 58% of random Kaggle frames land nearest the grassknoted *H* centroid; first-letter accuracy ~16–18% across every transform (raw-63, 2D, mirror on/off, ±features). No coordinate fix recovers it. **Key reframe:** the replay tests *grassknoted-kNN on Holistic landmarks* — a combination that never occurs in the real app (webcam → `HandLandmarker` → grassknoted-kNN is a **matched** domain, ~90%+ on careful signing per 25 user sessions). So the 0% does **not** mean fluid mode is broken on a webcam — it's unverified, needs a live test. **B3 revised:** the Kaggle data can't retrain the kNN; its real uses are (a) Stage 9 (train a sequence model *on* Holistic landmarks, matched-domain — what the comp winners did) and (b) its phrases as decoder stress-test material. **Next real test: turn on a webcam and try fluid mode.**
- **2026-09-04:** **M0 done + first real-data replay — the domain gap is the gate.** User pulled the Kaggle data; `data/fs_sequences.json` = 300 continuous sequences, 30 Deaf signers (names/addresses/phone numbers). New `tools/replay-lab.html` runs each through the full pipeline (normalize → kNN+heads → `transition.js` → `decode.js`) vs. the true phrase. **Result: 0% word accuracy, ~5% letter-LCS, flat across every `moveThr`/`stillThr` setting.** The architecture is sound (H, A emit correctly where the classifier cooperates; `mirrorX=true` is the right handedness for this set), but the grassknoted-trained kNN **mis-reads MediaPipe Holistic hand landmarks** — most per-frame letters come out M/X/F noise, and no segmentation or lexicon cleanup recovers that. So threshold tuning is moot until the classifier can read this domain. **Next: the B3 harvest pass — DTW-align the per-frame predictions to each known phrase, extract the held frames as `{label, v}` rows, retrain. Then re-run replay-lab.** `transition.js` threshold tuning waits for a classifier that works on the data.
- **2026-09-04:** **Technical push — the Stage 8 engine is largely built.** In one session: `decode.js` (benchmarked), `transition.js`, fluid mode wired end-to-end (transition→speller→decode→`speechSynthesis`), **measured confusion matrix** (`data/confusion.json` exported from `tools/test-knn.html`, blended over the hand-set floor via `mergeConfusion()` — real numbers n→m 13%, d→o 7%; re-benchmark with it: 8% letter err → **83% word / 60% sentence**, up from 79/45), **digit-run passthrough** in the decoder (phone numbers / addresses stay verbatim), **auto-speak** on a ~2.2 s pause, and **`tools/import_kaggle.py`** (ready to run — pulls `data/fs_sequences.json`). selftest 126→136. Deferred by choice ("pump the tech, fix bugs later"): threshold tuning of `transition.js`, live webcam verification, iOS `speechSynthesis`, the digit *classifier* (B6), the B3 harvest pass.
- **2026-09-04:** **Fluid mode wired end-to-end (Stage 8 Ph 3, the "fast path").** A "Fluid + speak (beta)" toggle in the spell panel (persisted): letters come from `transition.js` instead of the held-still stabiliser; `speller.js` keeps a raw `{letter,conf}[]` stream (`addLetter()` + `raw` getter, trimmed by backspace/clearPending/clear); `decode.js` (25k list, lazy-loaded) decodes it every ~350 ms into a "reads as" line; a 🔊 Speak button via `speechSynthesis`. Three-line panel = transcript + dashed pending, then decoded sentence + Speak. Non-fluid path byte-unchanged. selftest 134/134. **So "spell a sentence → hear it spoken" now exists end-to-end**, on synthetic-tuned thresholds. Not camera-verified — the live `transition → speller` flow and audio need a webcam on the deployed site. Next: user tests it live; then M0 (Kaggle) to tune `moveThr`/`stillThr` + the confusion matrix against real signing.
- **2026-09-04:** **`js/transition.js` built (M1's first item).** Rhythm-based letter segmentation: commits a letter when the hand *settles after a move* rather than waiting for a still hold; the move-between-settles rule preserves doubled letters; emits the majority letter over the settle window + its mean confidence (the posterior `decode.js` wants). DOM-free reusable-core module, selftest 132/132 (+6 synthetic: C-A-T→3 letters, B-move-B→"BB", long hold→one B, constant motion→nothing, low-conf→nothing). **Not yet wired into `speller.js`** — that's M3, after the `moveThr`/`stillThr` thresholds are tuned against real replayed sequences (needs M0). So the two hardest Stage 8 modules (`decode.js`, `transition.js`) now both exist and pass synthetic tests; the gate is real data + wiring.
- **2026-09-04:** **Stage 8 Ph1 prototyped + benchmarked — it works.** `js/decode.js` (trie / CTC-collapse / confusion-weighted beam / Norvig fallback) + `tools/decode-lab.html` built and committed. Benchmark on 40 sentences with synthetic ASL-confusion noise: **100% word acc on clean input**, ~79% at 8% per-letter error (vs ~64% naive), ~64% at 15% (vs ~46%) — the lexicon layer buys **+15–18 points**. Full-sentence exact drops fast (~45% at 8% err), confirming the raw/split/sentence UI + per-word override are load-bearing. ~55 ms/decode at beam 60. selftest 126/126 (+6 decode). Known gaps: double letters (needs `transition.js`), numbers (needs a digit path), and swapping the built-in confusion matrix for the real one from `test-knn.html`.
- **2026-09-04:** **Consolidated everything into a North-star roadmap toward an RIT showcase.** The user's goal is now explicit: build the whole thing (Backlog B1–B8 + Stage 8 + a Stage 9 prototype) to near-final, then demo to an **RIT / NTID ASL professor or the Deaf community**. New *North star* section: 5 milestones (M1 solid+honest foundation incl. a full accessibility pass + `transition.js`; M2 real learning tool — curriculum + receptive practice + word content; M3 data upgrade — Kaggle import + digit signs; M4 Stage 8; M5 showcase prep — PWA + about page + reach out to RIT) with effort estimates, a "showcase-ready" definition, a ~4–6-month part-time timeline (Stage 9 a further semester, best done *with* RIT), and a fast-path option. Backlog items re-slotted into milestones; B5 accessibility and B6 digits promoted to **High**; Goal section rewritten around the north star.
- **2026-09-04:** **Stage 9 added — the ambitious arc for "actually useful to the community."** Leads with *talk to Deaf people first* (confirm the problem; a solo project's best impact may be an open component + a consented Deaf-led landmark dataset). Two hard limits, each with a real attack: (1) **native-speed fingerspelling** needs a *sequence model on landmark input* — proven & on-device by the Kaggle competition (Squeezeformer + transformer decoder → TF-Lite); path = train a small CTC model (GRU/1D-CNN, ~100k–2M params) in Colab, run in-browser via ONNX Runtime Web, keep the kNN for Practice. The "no ML framework" rule is the only blocker and it's self-imposed. (2) **names/numbers** is a vocabulary+UX problem — context lexicons (Census names, address grammar, digit grammar), promote **B6 digit signs to high priority**, spell-back confirmation, session dictionary, entity priors. Semester-scale; sequenced after Stage 8.
- **2026-09-04:** **Stage 8 build-simulation added** — phased plan (Ph 0 Kaggle test-set → Ph 1 `decode.js` → Ph 2 `transition.js` → Ph 3 wire behind a beta toggle → Ph 4 harvest+retrain → Ph 5 UI+speech), each with an effort estimate (≈14–22 focused sessions total, 3–6 weeks part-time), a risk column, an explicit failure-modes list with fallbacks, and an efficiency section (everything offline-testable against the replayed Kaggle sequences; `decode-lab.html` for keyboard-speed iteration; one word list two uses; ship behind a toggle).
- **2026-09-04:** **Folded prior-art tactics into the plan** (after checking Stage 8 isn't a novel idea — Sign2Text, the Google Kaggle comp, and continuous-fingerspelling papers all do variants). Three concrete borrows: (1) **B3 rewritten around the [Google Kaggle ASL Fingerspelling dataset](https://www.kaggle.com/competitions/asl-fingerspelling)** — public MediaPipe landmark sequences from 100+ Deaf signers, drops into our pipeline, fixes M/N/D *and* gives Stage 8 its continuous test set (task: `tools/import-kaggle.html`). (2) **`js/transition.js`** added to Stage 6 follow-up — segment letters by the settle→move→settle rhythm instead of a still hold; the real fix for "Spell mode isn't fluid." (3) **Stage 8 pipeline rewritten** from "segment then correct" to **trie + CTC-collapse + beam search through a lexicon**, with per-letter emission costs weighted by the measured confusion matrix — the lightweight form of what the Kaggle winners do. Prior-art + honest-accuracy notes added to Stage 8.
- **2026-09-03:** **Plan redirect toward "word ASL via fingerspelling" + a competitive-gap backlog.** Two changes, from the *Signing to a Webcam* competitive read (`scratchpad/fingerspelling-landscape.html`): (1) new **"Backlog — catching up with the field"** section — every capability competitors ship that we don't (receptive practice, a real curriculum, Deaf-signer data, curated word content, app accessibility, number signs, accounts/sync, PWA), priority-ranked. (2) new **Stage 8 — fingerspelling → sentence → speech**: lean into what Spell mode already does — capture the raw recognised letter stream, then a plain-JS pipeline (dictionary + DP word-segmentation à la Norvig, confusion-aware fuzzy correction, sentence assembly) turns "whatareyoudoing" into "What are you doing?", shown live and spoken via the browser's `speechSynthesis`. Not real ASL word-sign recognition — a slow-spelling approximation that needs no word-sign data. Scope note updated.
- **2026-09-03:** **Spell mode — pending-word buffer (the real "not fluid" fix).** Per-letter auto-commit was structurally wrong: every stray frame while the hand moved landed a permanent letter and swipe could only undo one. New model: recognised letters build a **pending word** (rendered dashed, `<span class="sp-pending">`) — a draft that drops into the transcript only on a ~1 s pause (word break) or a Space tap. `js/speller.js` rewritten: `text` + `pending`, `feed() → {text, pending, event:"letter"|"word"}`, `display`/`pending` getters, `clearPending()`, reworked `space`/`backspace`/`insert`/`clear`. **Swipe now = scrap the whole pending word** (not "delete one char") — the junk a moving hand adds gets wiped by the same motion; ⌫/backspace hits pending first, transcript only when the draft is empty; copy grabs `display`, paste commits the current word then inserts. Help + hint rewritten. Self-test 119/119.
- **2026-09-03:** **Spell gestures — fix "not firing" on real hands.** (1) **Swipe-to-delete** was fed the EMA-smoothed landmarks, which damp exactly the fast motion a swipe is; now fed **raw** landmarks. Thresholds loosened: sideways travel 1.6→1.1 spans, horizontal dominance 2.2×→1.6×, open-fraction 0.7→0.5, "open" = 3/4 fingers extended + light spread (was all four fanned), window 440→650 ms, buffer survives a 450 ms frame-clip. (2) **Two-hand copy/paste** — MediaPipe merges/drops a hand track exactly when the hands meet (the moment "copy" needs). Rewrote `match()` to use the widest & narrowest gap seen between two *open* hands and their **order** (apart→together = copy, together→apart = paste) instead of first-third/last-third averages, so dropped frames in the middle don't matter; "open" loosened to 3/4 fingers; window 560→750 ms. (3) **Live readout** under the controls (`#spMetrics`): `swipe: sideways X/1.1 · up-down Y · open Z` with one hand, `hands: gap X (saw min–max)` with two — so a gesture that still won't register can be tuned to real numbers. (4) The gesture-demo box also animates **Add a letter** (hold still, ring fills) and **Space** (lower the hand, a gap appears). Self-test 116/116.
- **2026-09-03:** **Spell mode — animated gesture demos.** New "Hand gestures — how to do them" fold (open by default) with a small looping CSS animation per gesture: an open hand sweeping sideways (delete), two hands together (copy), two hands apart (paste), + a "fingers must be spread" note. `prefers-reduced-motion` freezes them at a legible pose. "How to spell" (text steps) collapsed by default now that the gesture block owns the how-to. `serve.ps1` also wraps `GetContext()` so a listener hiccup can't end the loop. Self-test 114/114.
- **2026-09-03:** **Spell mode — two-hand copy / paste gestures.** Uses the 2nd tracked hand (`NUM_HANDS` was already 2). New `js/twohand.js`: buffers the wrist-to-wrist gap between two *spread-open* hands over ~560 ms → `"copy"` when they come together (gap > 2.4 spans → < 1.6) or `"paste"` when they pull apart (mirror), 800 ms cooldown; requires both hands spread-open so one-handed spelling can't fire it. Copy also writes the system clipboard (Ctrl/Cmd+V anywhere) and stashes the text locally so paste works even when the clipboard API is blocked; paste re-inserts via new `speller.insert()`. Both hands are now drawn on the overlay in Spell mode. Added a **Paste** button (tries the real clipboard, falls back to the local stash). Self-test 114/114 (+7 twohand, +1 speller.insert).
- **2026-09-03:** **Spell mode — swipe-to-delete gesture.** New `js/swipe.js` (`createSwipeMatcher`): buffers the wrist path + a "spread-open hand" flag over ~440 ms, fires `"delete"` when a fanned-out hand ("5", not a flat "B" — checked via 4 fingers extended AND adjacent-tip gaps summing > 0.9·span) sweeps > 1.6 hand-spans horizontally and clearly more sideways than vertical, then a 650 ms cooldown (one wipe = one delete). `main.js` runs it only in Spell mode → `speller.backspace()` + `spellStab.reset()` (so the flat hand doesn't then commit as a letter) + red flash/buzz. **Copy/Clear stay buttons** — a pinch is literally the letter F so it would misfire constantly, and "paste" has no target inside a single screen. Self-test 106/106 (+6 swipe checks).
- **2026-09-03:** **Spell mode — in-panel help + A–Z reference chart.** The spell panel now carries a `<details open>` **"How to spell"** (add a letter / next letter / doubled letters / space = pause or button / J-Z strokes / Delete-Clear-Copy are buttons, no hand sign) and a collapsible **"Alphabet chart"** — a 26-cell grid of the reference photos, tap any to open the enlarged animated demo (reuses the practice zoom; plays the stroke for J/Z). Ring + transcript + controls are a sticky header inside the panel so they stay put while the help/chart scroll; panel caps at 46vh (portrait) / row height (landscape) with internal scroll. Also fixed `serve.ps1` crashing the request loop on a HEAD/disconnect (`ContentLength` ProtocolViolation) — per-request try/catch, explicit `ContentLength64`, bodiless HEAD; added `.claude/launch.json`. Self-test 100/100.
- **2026-09-03:** **Spell mode — continuous fingerspelling → text (Stage 7, first cut).** A third mode next to Practice/Challenge. New `js/speller.js` = a pure state machine: `feed({holding, letter, stroke, handPresent, moved, now})` → `{text, pending, event}`. Rules: a **settled letter commits once** (own faster stabilizer — `stableFrames:5, minConfidence:0.5`); a **different letter** needs no gap, the **same letter repeated** needs the hand to leave the shape (`gapMs 350`) or a notable wrist shift (`moved`, >0.14 normalised); a **pause of `spaceMs` (1100 ms)** with no letter forming inserts one space; **J/Z** commit from the existing `motion.js` stroke matcher. `main.js` runs it in the loop, draws a live fill-ring for the building letter, and wires Space/Backspace/Clear/Copy (+ keyboard Backspace/Space in this mode). Mode persists (`asl-pref-mode`). UI: `#spellPanel` beside the camera (landscape) / below it (portrait), verified no h-scroll at 375 px. Self-test **100/100** (+6 speller: CAT, single-commit, BOOK double-letter, one-space-per-pause, JAZZ strokes, backspace/clear/space). Next: tune the hold/gap timings against real signing, consider an explicit open-hand "space" gesture, and a confidence floor so junk frames don't commit.
- **2026-09-03:** **Handedness / mirror / orientation audit** (user: "check for bugs and test everything, especially that the hand mirrors and is oriented correctly — that's been a long issue"). **Front camera (the validated path): verified end-to-end, unchanged.** `mirrorX = MIRROR_LEFT_HAND && left` matches the dataset build exactly — a synthetic right hand and its left-hand mirror both classify to the same letter at conf 1.0; `reference.orient()`/`score()` resolve canonical→`mirrored:false`, x-flip→`mirrored:true`, a 12° tilt→a same-sign `deg` correction (locked in a new selftest for G/H/P); the guide's `mirror` param `(left) !== o.mirrored` plus the `.stage` `scaleX(-1)` flip stay consistent for either hand and either camera. **Bug fixed — back camera:** the earlier "back-camera support" change inverted `isLeftHand` when `facingMode === "environment"` on the premise that the back feed is "the mirror image" of the front. It isn't — MediaPipe reads handedness from raw pixels and doesn't know the camera, and its label was validated against the real hand on the front camera. With the inversion, `mirrorX` is backwards on the back camera and asymmetric letters classify as their mirror (tested: G→I @0.4). Removed it (`isLeftHand = rawLeft` always); the back camera's real differences (no selfie flip on `.stage`, reference-flip direction in `applyHand()`, burst origin in `reward()`) are still handled separately and keyed on `facingMode`. **NEEDS a real back-camera device test to confirm**; the manual Hand toggle overrides if a device ever reports the label backwards. **Also fixed:** `applyHand()` now runs on every `setTarget` (was leaving a stale panel `.mirror` when switching between a static letter and J/Z with no hand-change event); the J/Z practice stat line falls back to per-letter stats instead of blanking when no hand is mid-stroke. Self-test 94/94.
- **2026-09-03:** **J/Z practice made repeatable + clearer + un-mirrored.** Live feedback: "I can only practise it once — the second swoosh does nothing; it's not clear when I complete it; the Z demo is backwards." Fixes: (1) **Repeatable** — the `rewarded` flag was set on the first stroke and never cleared for motion letters (static letters clear it when the sign breaks; a stroke has no "break"). It now re-arms ~1.5 s after the reward and clears the motion buffer, so you can trace J/Z over and over; each rep gets a fresh completion-time clock. (2) **Clear completion** — the motion path now drives the *same* fill bar + rising "charge" tone the held letters use: it grows with the live stroke metrics (`min(pinkyMove/1.2, pinkyDrop/0.7)` for J; `min(indexMove/1.2, indexX/1.6, rev/1)` for Z) and snaps to full green on a hit, on top of the existing particle burst + chime. (3) **Un-mirrored demo** — the reference panel was getting the `.mirror` flip for the tracked hand, which renders the Z stroke as a *backwards* Z. `applyHand()` now never mirrors the panel for J/Z, so the demo always plays a clean readable forward Z / J. Rewrote the `LETTER_GUIDE` J/Z text in plain "your hand" terms ("draw the Z the way you'd write one: across, down-slash, across"). Self-test 93/93; app boots clean.
- **2026-09-03:** **J/Z motion fix — held handshape was falsely completing; the swoosh often wasn't.** From live feedback: "if I make the hand sign before doing the motion it registers, and it *doesn't* register when I try to make the swoosh; also the skeleton overlay moves when you swoosh so the guide is useless." Root cause: `motion.match()` measured `pinkyLen`/`indexLen` as the summed frame-to-frame step length, so ~40 frames of MediaPipe landmark jitter *accumulated* past the (very low) thresholds even on a perfectly still I-hand/pointing-hand → instant false "J"/"Z". Fixes: (1) `motion.js` — replaced path-length with **`maxExcursion`** (farthest the tip ever got from its start point) + axis **`range`** for the horizontal sweep; both are immune to per-frame jitter. Raised the bars to a real stroke: **J** needs `pinkyMove > 1.2` span **and** `pinkyDrop > 0.7`; **Z** needs `indexMove > 1.2`, a `1.6`-span horizontal range, **and** ≥1 real reversal (reversal step threshold `0.015 → 0.12` so jitter isn't counted as a "turn"). (2) `main.js` — feed `motion.push()` the **EMA-smoothed** landmarks, not raw, so idle jitter is damped before the buffer; practice readout now shows each metric against its threshold (`move 1.4/1.2 · drop 0.9/0.7`). (3) `overlay.js` — the on-camera swoosh arc is now **anchored at the wrist** (STROKE is already wrist-relative), not the moving fingertip, so it stays put while the hand pivots through the stroke. Self-test 93/93 (held-hand-with-jitter fires nothing; a real J/Z path still fires). Verify the feel on the deployed site and report the on-screen numbers if a real stroke still won't register.
- **2026-09-03:** J/Z detection made much more forgiving + a tuning readout. Rewrote `motion.match()` around simple metrics — pinky/index path length, downward drop, horizontal travel, direction reversals — with very low bars (`pinkyLen > 0.7`, `pinkyDrop > 0.2`, `indexX > 0.6`, `rev ≥ 1`) and a soft "which finger moved most" fallback when the "finger extended" check is borderline. New `motion.metrics()` returns the live numbers; practice shows them under the meter (`pinky up 0.9 · path 2.1 · drop 0.8`) so a deliberate swoosh's values are visible for further tuning. Self-test 93/93.
- **2026-09-03:** Mac layout fix + J/Z follow-through. (1) **"Super zoomed" camera on Mac** — the `@media(landscape){ .viewport{ width:min(...) } }` rule sat *before* the base `.viewport{}`, so equal-specificity source order let the base win: the camera became `flex:1` + `width:100%` + `aspect-ratio:4/3` = as wide as the row and taller than the window, shoving the letter grid (incl. Z) out of reach. Moved the override after the base rule, added a `@supports not (height:100dvh)` vh fallback + a max-height cap. Verified 4:3 and in-window at 1100×680 / 1280×800 / 1440×900. (2) **J/Z detection loosened** — `extended` threshold 0.9→0.72, drop 0.5→0.32, hook 0.28→0.16, Z travel 1.4→1.0, `MIN_FRAMES` 8→6, window 1.4→1.6 s. (3) **Motion demo now shows a hand skeleton** doing the swoosh (start handshape `I_HAND`/`POINT_HAND` translated along the stroke), not just a dot. (4) **On-camera swoosh guide** — new `overlay.drawMotionGuide()`: the live skeleton + a purple dashed arc (the stroke scaled to the hand, anchored at the tracing finger) with an arrowhead and a dot animating the direction; shown for J/Z when the guide toggle is on. Self-test 93/93. Still tune the J/Z thresholds against real strokes.
- **2026-09-03:** **J and Z added — the two motion letters (Stage 6 part 1).** J is the "I" hand tracing a hook, Z is the index finger drawing a zigzag, so neither goes through the static shape-match path. New `js/motion.js` (`createMotionMatcher`): a ~1.4 s ring buffer of the tracked fingertip, normalised to hand span, with heuristic matchers — **J** = "I"-hand pinky tip drops then hooks sideways; **Z** = index-up tip zigzags (≥2 x-reversals, mostly horizontal). Fires once per stroke then arms a ~0.9 s cooldown. Wired everywhere: the picker is now the full **26 letters** (`config.ALL_LETTERS`), J/Z chips are tinted purple with a ↻; picking one shows the photo + a **stroke demo** (a dot tracing the letterform, `player.setMotion()`) and completes when you trace it — no hold. A→Z run and Challenge both include them (`seen = tracedStroke || stabilizer.current`). The classifier/heads/`reference` are untouched (still 24 static letters). Pulled J.jpg / Z.jpg from the grassknoted source. Self-test 92/92 (+6 motion checks). NOTE: the J/Z thresholds are first-pass — tune against real strokes on the deployed site.
- **2026-09-03:** **Mirror-tolerant matching** — from live feedback: "to register a sign I have to turn my hand the opposite way, and my wrist can't physically roll there." A left hand doing a sign is the *mirror* of the right-hand template, and you can't pronate/supinate into that. Fix: `normalize.mirrorVector()` + `reference.bestOrientation()` — `score()` and `regionErrors()` now try the live hand **and its x-mirror** (each with the existing ±22° tilt forgiveness) and take whichever fits. `score()` returns `mirrored`; the new `reference.orient()` feeds the on-camera guide the winning mirror + tilt so its arrows point where the wrist *can* go (`main.js` passes `mirror: base XOR o.mirrored`). Verified: mirrored R/P/L training samples now register at the same rate as normal (13/15, 15/15, 13/15); a mirrored letter-shape scores "correct" with `mirrored:true`; wrong letters still read "off". Self-test 87/87 (+2 mirror checks).
- **2026-09-03:** **M/N/D accuracy fixed — learned refinement heads.** The tie-breaker-by-single-measurement was disproven earlier (best M-vs-N separation 65 %), but a *learned* combination over the whole 74-dim standardised feature vector works. New `js/heads.js` + `js/heads.json` (~93 KB): two small ensembles of three one-hidden-layer MLPs (74→24→K) — one for **M/N**, one for **D/O/C** — trained in plain JS (no new dependency) by `tools/train-heads.html`. At runtime a head is consulted **only when kNN itself landed on a label it covers**, so fast kNN still owns the other 21 letters. Group-aware 20 % held-out (`tools/mlp-lab.html`): **kNN 95.9 % → kNN + heads 97.1 %** — M 85→92, N 84→96, D 87→97, O 90→97, nothing regresses; every letter is now ≥ 92 %. Wired into `main.js` after `classify()`, before the stabiliser, so the corner badge, the challenge win-check and the practice "readable if recognised" path all benefit. Loads in the background; the app is unchanged if `heads.json` is missing. Self-test 85/85 (+5 heads checks). Kept: `tools/mlp-lab.html` (honest held-out A/B) and `tools/train-heads.html` (re-export if the dataset changes).
- **2026-09-03:** Back-camera support. The front (selfie) camera stays mirrored on screen; the **back camera now shows the world un-mirrored** (`.viewport[data-facing]` set from `facingMode`). Switching cameras reverses the mirror relationship, so on the back camera every hand-side reading flips — `left` (classification mirror), `realHand` (the tag), and the reference-flip direction in `applyHand()` all invert when `facingMode === "environment"`; the reward-burst origin too. Front-camera path is byte-identical to before. Manual Right/Left toggle still overrides. Self-test 80/80.
- **2026-09-03:** Feature batch — ten UX/feedback additions. **First-visit "How it works"** 3-step overlay (dismiss stored). **Harder letters (M/N/D) tinted amber** in the grid with a dot + tooltip. **Challenge lives + Skip** — 3 hearts; a timeout or a Skip (–1 ♥) costs one, out of lives ends the run; "✕" miss beat between letters. **A→Z "Next X" bridge** — a 1.3 s card between letters instead of an instant jump; and an **A→Z results card** at the end (fastest 3 + any that took >8 s). **Keyboard** — ←/→ step letters, A–Z jump, Space starts/skips the challenge, Enter dismisses the intro. **Tap-to-enlarge demo** — the panel diagram opens a full-screen player. **Haptics** — `navigator.vibrate` on completion, challenge win, miss, and game over. **Remembered setup** — last mode, last letter, hand override persisted (`asl-pref-*`); restored on load. **Per-letter stats** — completions + best time under the meter, kept in `localStorage`. **"Stuck" assist** — 12 s on one letter without landing it replays the demo and pulses the panel with a "Still tricky?" hint. **Recogniser readout in practice** — a calm dot + "recognised / reads X" line so you know the classifier agrees, not just the shape meter. Self-test 80/80.
- **2026-09-03:** Real-time hand tracking + clearer options UI. (1) **Auto hand-detect now follows the camera live** — a 3-frame confirm instead of a sticky ±3 vote, so swapping hands mid-session (A→Z run, practice, challenge) re-orients the reference within ~100 ms. Manual Right/Left override still available. (2) **Options made prominent** — the practice sub-mode is now a **Free pick / A→Z run** segmented toggle (matching the Practice/Challenge one), both enlarged with an on-state glow; the A→Z progress is a pill; the bottom **controls row is a proper bordered toolbar**, hidden until the camera's on. Self-test 79/79.
- **2026-09-03:** Handedness alignment fix + quick letter stepping + A→Z run. (1) **Handedness was mislabelled/misaligned** (tester: "my right hand only worked held like a left, and the reference showed opposite"). We feed MediaPipe a *non-mirrored* frame, but it reports handedness assuming a mirrored/selfie one — so its label is the *opposite* of the real hand. The reference photo (a right hand, observer view) is the horizontal flip of what a right-handed signer sees of themselves in the selfie-mirrored camera, so the reference is now flipped **for a RIGHT hand** and left as-is for a LEFT hand, and the "· right/left hand" tag finally names the real hand. Added a **Hand: Auto / Right / Left** control in the toolbar as a manual override. The classification mirror rule (`mirrorX` keyed on MediaPipe's raw "Left") is unchanged — it matches the dataset build and is internally consistent. (2) **Compact letter strip** gained ‹ › **step arrows** (bigger mobile tap targets than the grid) and a larger current-letter readout. (3) **A→Z run** — a practice sub-mode (toggle in the learn bar): starts at A, auto-advances to the next letter each time you complete one, shows `5 / 24` progress, turns finished chips green, and celebrates "Alphabet complete!" at the end. Free-pick mode is unchanged. Self-test 79/79.
- **2026-09-03:** Challenge mode fleshed out ("kinda lacking and empty; advance when it can actually detect the letter, not an uncertain shape guess"). **Win condition is now the recogniser** — you advance when `stabilizer.current` (the debounced, N-sure-frames classifier call) reads your hand as the target letter, not the shape meter. The stabiliser resets when the play phase starts, so the letter must be formed *fresh* during the round. Added: a **"GO!" flash** phase between study and play; a live **"seeing: X"** readout so it's not a black box; **speed-based scoring** (10–30 pts/letter, faster = more) with a floating "+N"; a **streak** counter (🔥3); a **low-timer** state (bar thickens + pulses red + ticking `sound.tick()`); a richer game-over card ("Run over" — score, best, round reached, the letter you missed) with `sound.fail()`. Self-test 79/79 (state machine: study→go→play→win-by-letter→over, wrong letter never advances, best + missed-letter recorded).
- **2026-09-03:** Relaxed "correct" to a *readable* sign, not a perfect one ("holding it out to get it pixel-perfect is tiring"). (1) `reference.score`: "correct" is now `s ≥ 0.70` (was 0.85) with the worst joint allowed to `1.8× tol` (was strict `tol`); "close" starts at `s ≥ 0.5`. A letter's own training samples now land ~57% "correct" (was ~47%), and a clearly-wrong finger (several × tol) still fails. (2) `main.js`: if the shape is only "close" but the **recogniser confidently reads it as the target** (`confidence ≥ 0.6`), that counts as correct — a functioning sign. (3) Hold shortened `1600 → 1150 ms`, grace `220 → 260 ms`. (4) The on-camera guide stops highlighting the worst finger once the sign already counts (`settled`) so you're not nagged toward perfection. Self-test 78/78.
- **2026-09-03:** Usability pass from live feedback. (1) **Challenge Start button did nothing** — `.ch-card` sat inside `.hud` (`pointer-events: none`) and never got it back; added `pointer-events: auto`. Also: the challenge now auto-begins as soon as the camera is up (`pendingChallengeStart`), so it's one click not two. (2) **Onboarding** — a bobbing "👆 Pick a letter above" bubble shows on the camera whenever practice mode has no letter chosen; the pre-camera copy now names both modes. (3) **Letter picker reorganised** — moved out of the cramped top bar into its own full-width row that *wraps* (all 24 visible, no horizontal hunting), and it **collapses to a compact "Learning R · change" strip** once a letter is picked. (4) **Guide legibility** — the correction overlay no longer scatters white rings across every off joint; it now **highlights the single most-off finger in bright yellow along its whole length**, draws one bold dashed lead to a filled pulsing destination, and **labels it with the finger name** ("ring") right on the video. The hint text matches ("Adjust your ring finger — follow the yellow marker"). Self-test 76/76.
- **2026-09-03:** Challenge mode (Stage 6-ish) — a "Simon says" speed game. New `js/challenge.js` state machine (`study → play → won → over`): a random letter shows with its demo for ~2.6 s, then all help disappears and a shrinking timer bar runs; form the letter in time → +1 and the next round is faster (`roundDur = max(2s, 5.5s − round·0.22s)`), miss it → game over with score + a `localStorage` best. A **Practice / Challenge** segmented toggle in the header (`#modeToggle`); challenge HUD = top timer bar (`--time`), corner score badge, big centre letter during play, a start / game-over card. The correction guide, meter, hint and recogniser badge are all suppressed in challenge. Also fixed a latent bug found while wiring it: author `display` on `.reference` / `.learn` beat the UA `[hidden]` rule (equal specificity), so hidden panels could still render — added `[hidden]{display:none!important}`. Self-test 76/76 (full-round + stop() checks).
- **2026-09-03:** Endgame guidance — "near perfect but I can't see the mistake". Two parts:
  1. **The on-camera guide now shows where to move, not just a colour.** `overlay.drawGuide` draws a faint dashed **ghost of the exact target shape** anchored to your wrist, and for **every joint still outside tolerance** a dashed lead line from where it is to a ring at where it should be — "move this dot onto that dot", live-updating as you adjust. The single worst joint gets a white pulsing marker + arrowhead so the eye goes to "fix this first". It returns that joint so the text hint can name it ("Nudge your thumb tip onto the marker") instead of falling back to "looks right" when the coarse feature check runs out.
  2. **Casual wrist tilt is forgiven.** A slightly tilted sign was reading as error everywhere (the guide compared to the upright canonical shape), so "near perfect" was unreachable when the hand was cocked a few degrees. `reference` now estimates the hand's tilt from the wrist→mean-of-MCPs axis and rotates the target to match, clamped to **±22°** and only applied when it actually reduces the distance (so a noisy axis estimate can't invent error). Used consistently by `score`, `regionErrors` and the guide (`reference.alignDeg`). A real mis-orientation (G/H sideways, P down) is tens of degrees off and still reads wrong. Verified: a 12°-tilted T now scores "correct" (was penalised); the z-noise regression still holds. Self-test 74/74.
- **2026-09-03:** Practice-clarity pass, from live testing ("instructions get vague on harder letters; the reference is a static image not a demo; the camera skeleton looks stringy; want a ramp-up sound while holding"). Four changes:
  1. **Animated reference skeleton** (`reference.createCanonicalPlayer`) — the panel canvas now *plays* the sign: the hand eases from a relaxed open pose into the target letter, holds, resets, loops, with a faint static target underneath showing where it's heading. It's a short "how they did it" clip instead of a still. Reduced-motion → static final shape. Replaces the direct `drawCanonical` call in `main.js` (that export stays for the selftest).
  2. **Plain-language descriptions** (`reference.describe()` + `LETTER_GUIDE`, 24 entries) — an evocative one/two-sentence "how to shape it" with a real-world image where there's a good one ("D: index up, other tips meet the thumb — a candle above its holder"; "F: thumb-and-index circle, other three up — the OK sign"). Shown in a new `#refDesc` line in the panel whenever a letter is selected. Live corrections (`hint()`) also reworded fuller ("Curl your ring finger further down toward your palm", "Close the gap — bring your index and middle together until they touch").
  3. **Skeleton rendering cleaned up** — `drawSkeleton` and the on-camera guide now size strokes/joints to the hand's on-screen span (not a fixed 4 px web), draw a dark contrast halo under every bone and dot so it reads on any background, and give knuckles/fingertips distinct sizing. Plus **landmark EMA smoothing** (`smoothLandmarks`, α 0.5) on the raw tracker output before drawing/normalising — kills the frame-to-frame jitter that made it look "stringy" and steadies the meter.
  4. **Ramp-up hold sound** (`sound.charge(p)`) — a low tone that rises in pitch and volume with the hold progress 0→1, resolving into the existing `success()` chime + burst. Stops cleanly on release, mute, or camera-off.
  Self-test 71/71 (added: `describe()` covers every letter, `createCanonicalPlayer` API, `sound.charge`). NOTE: the animation, smoothing and charge sound can't be seen/heard in the automation browser (hidden pane freezes rAF + synthetic camera) — verified by unit tests, direct-draw ink checks, and the player animating once the pane was fronted; confirm the feel on the deployed site.
- **2026-09-03:** Reward tightened + a real hold buffer, from live testing ("it rewards you when you *almost* do it — for M it was still telling me to move fingers but an accidental pose fired the reward; I'd rather hold the sign for a couple seconds so a slip doesn't count"). Two changes: (1) **"correct" now needs the skeleton *fully* green, not just a good average.** `reference.score()`'s "correct" bucket previously only needed the aggregate distance under p50, so a sign that was close on average but had one finger clearly out (the classic M) still counted. It now also requires every joint's x/y error ≤ the same per-joint tolerance the guide uses to lock a segment green — so "meter complete" == "skeleton all green". Verified: one finger at ~3× tolerance now reads "close"/`matched:false` (was "correct"); all-joints-green still reads "correct". Added `matched`/`worst` to the return; shared the tolerance calc (`tolFor`) so `score`, `tolerance`, `regionErrors` and the guide can't drift apart. (2) **Time-based hold** — replaced the 6-frame (~0.2 s, felt instant) counter with `HOLD_MS = 1600` of a *sustained complete* sign before the reward, with a `HOLD_GRACE_MS = 220` window so brief tracking flicker doesn't reset it. Feedback while holding: a green "charge" bar fills along the bottom of the camera (`--hold` CSS var) and the hint shows `Hold it…  ●●●··`. Self-test 69/69.
- **2026-09-03:** Match meter could never complete — **dropped z from the practice-match metric.** From live testing: "meter stuck a bit past half, skeleton fully green, hint said 'looks right, hold steady', held it and nothing happened." Cause: `reference.score()`'s distance-to-centroid summed all 63 coords **including z**, while the guide overlay, the hints, and the reference photo are all 2-D. MediaPipe's z is a single-image depth guess whose scale differs between the training photos and a live webcam, so a live hand carried a large, invisible, unfixable z error that held the score in the "close" band even with a perfect on-screen match — and since "correct" was never reached, the reward never fired. Fix: `coordDist` (and `regionErrors`) now use landmark **x/y only**; the p50/p85 self-calibration recomputes on the same metric, so "every joint green ⇒ total error ≤ p50 ⇒ correct bucket" now actually holds. Verified: an x/y-matched hand with heavy z noise now scores 0.90–0.97 "correct" (was "close"/"off"); wrong-letter hands still read "off" (0–0.28); own-sample bucket mix unchanged (47/33/21). Also: the hint no longer says "looks right" while the meter is short — it says "so close — tiny adjustments". Self-test 69/69 (added a z-noise regression check).
- **2026-09-03:** Universal one-screen layout + visible idle ambience, from feedback ("make the window fit any device so aspect ratio isn't a factor; the UI/colour breathing effects aren't visible from the start"). **Layout:** `body` is now a flex column pinned to `100dvh` with `overflow: hidden` — header + workspace + controls always fill exactly one screen on any device, no page scroll, no dependence on the camera's aspect ratio. The camera box `flex: 1`s into whatever space is left and the `<video>` + overlay `<canvas>` both `object-fit: cover` it (they crop identically, so the skeleton stays aligned at any box shape). Removed the two inline `viewport.style.aspect-ratio` sets in `main.js` that were forcing the box to the camera's ratio. Reference panel switches by orientation: **portrait** → a short horizontal strip under the camera (photo + skeleton + meter in a row); **landscape** → a compact card at the top-right (`align-self: start`, no more tall empty box). Verified no scroll at 375×812, 1366×768, 1440×900. **Ambience:** `bg.js` now renders a calm *breathing* cyan field at idle (was near-invisible dark-warm — read as flat black in screenshots). Idle blobs: cool hue ~198, gently pulsing energy/alpha, slow drift; verified animating with pixels ~rgb(30,63,83) vs the flat rgb(15,23,42) base. Added an `html` depth gradient and a soft idle breathing glow on the camera frame (`vp-idle-breathe`, reduced-motion-gated). Self-test 68/68. **Follow-up (same day):** on landscape/desktop the camera reverted to a true **4:3** box (width = row-height × 4/3, capped 900px, shrinks on small screens) with the camera + reference card **centred in the row, equal margins** — the earlier "fill the row" version made a wide webcam look near-square. Reference card centres vertically beside the camera. Portrait/mobile unchanged. Verified 4:3 + centred + no scroll at 1280×720 / 1366×768 / 1440×900.
- **2026-09-03:** Fit-to-viewport + reference-panel trim, from live feedback ("have to scroll up/down on the computer; the photo + skeleton boxes are blocky and big"). (1) **No page scroll on desktop** — the camera box is now capped at `min(82vh, calc(100vh - 190px))` so header + camera + controls fit inside a laptop window; the cap had to move *after* the base `.viewport` rule because equal-specificity CSS is decided by source order and the base `max-height: 82vh` was overriding it. Video stays full via `object-fit: cover` (crops a few px top/bottom at short heights — accepted tradeoff vs. scrolling). (2) **Reference thumbnails** capped at 140px and centred, softer canvas border, panel rounded more — they no longer tower beside the camera. (3) `sizeRefCanvas()` defers one rAF after the panel un-hides so it measures a real width, with a sane non-zero fallback. Verified at 1280×680 and 1366×768: no page scroll, both thumbnails render. Self-test 68/68. **NOTE:** could not reproduce the user's "photo box empty/black" on the live deployed site — the image loads and the skeleton draws with 0 console errors; most likely a stale browser cache on their end (advised: private window or `?v=` query). The colour / glow / meter / ambient-background feedback only runs while the camera is on and a hand is in frame — no camera = no feedback, by design.
- **2026-09-03:** Reactive ambient background (`js/bg.js`) — a full-page canvas of slow-drifting soft blobs behind everything, colour eased warm→amber→green by the live match score. **Spatial:** blobs are anchored to screen regions (top / thumb-side / pinky-side / centre) and `reference.regionErrors()` reports per-zone error, so a wrong region stays warm and drifts *more* while correct regions go green — the background points toward the problem (verified: B with bent fingertips → top zone error 0.77; thumb moved → left zone 0.53; on-target → all zones 0). Respects `prefers-reduced-motion` (drift frozen, colour kept). Body background moved to `html` so the canvas shows through. Self-test 68/68.
- **2026-09-03:** Reward + polish pass ("it works but doesn't *feel* like anything"). **Reward on completion:** hold "correct" for ~0.2 s → a particle burst from the hand (`js/fx.js`, full-viewport canvas, self-sleeping rAF), a green screen glow, a rising 4-note chime (`js/sound.js`, Web-Audio synth — no assets, unlocked on the camera-button click), the viewport pops-scales, the letter badge bounces with a spin, and a "Nailed B! ✓" toast slides up. Sound mute toggle (🔊/🔇) in the controls, persisted in localStorage. **UI polish:** gradient panels with depth/shadows, chips lift on hover + scale+glow when selected, breathing glow on the CTA, gradient/shimmer meter, ambient background glows, spring easings throughout. All motion respects `prefers-reduced-motion` (burst + big animations off, colour feedback kept). New modules `js/fx.js`, `js/sound.js`. Self-test 64/64; reward visual layer verified piece-by-piece (`pop-scale` / `badge-pop` / `meter-shimmer` animations present, toast slides, fx canvas draws 115k particle pixels). The live *trigger* (hold-counter in the loop) is unverified in-session — check on the deployed site.
- **2026-09-03:** Practice feedback overhaul, from live testing ("guide goes green but meter stuck on 'close', and I don't know what to fix"). (1) **The meter could never reach "got it"** — it secretly also required the classifier's top-1 to equal the target, which for M/N/D often fails even with a good hand. Removed that gate; the meter is now pure shape-match. (2) **Thresholds were miscalibrated** — the old fixed 0.82 "correct" cutoff demanded the hand be closer to the class mean than ~99% of *training* samples were. Now self-calibrated per letter: p50 of that letter's own spread = "correct", p85 = "close". Verified: a letter's typical training samples now land ~50% correct / 35% close / 15% off (was ~0% correct). (3) **Plain-words hint** (`reference.hint()`) — picks the single most-off thing from the rotation-independent engineered features and says it: "Curl your ring finger in more" / "Bring your index and middle closer" / "Tuck your thumb in tighter" / "Looks right — hold it steady". Shows in the panel under the meter; if the classifier is confidently reading a different letter it appends "(reading as X)". (4) **Guide colour ramp** now scales to each letter's tolerance so "almost" stays amber instead of blending into green; on-target segments lock to bright thick green; correction arrows use per-letter thresholds. Reference panel: photo + skeleton now side-by-side (shorter). Removed unused `config.MATCH_CLOSE/CORRECT`. Self-test 59/59.
- **2026-09-03:** Deployed to GitHub Pages — **https://crazycatz67.github.io/asl-recognizer/** (repo `Crazycatz67/asl-recognizer`, `main` branch, `/` root). All assets verified serving with correct content-types; app boots (classifier + reference build) with no console errors. Updating = `git push`. `data/_src/` (794 MB source images) excluded via `.gitignore`.
- **2026-09-03:** Practice-view redesign, from live testing feedback ("skeleton + camera + ghost all stacked, hard to see"). (1) **Side-by-side layout** — camera is the hero (bigger, `max-height: 82vh`); on ≥760 px with a target selected, a **reference panel** sits beside it (stacks below on phones). (2) The reference panel shows the **photo (always on when learning, no toggle)** + a large clean **canonical-skeleton diagram** (`reference.drawCanonical`) in its own canvas — the "make this" is no longer a ghost tangled with your live skeleton. (3) **Feedback moved off the video**: the camera frame glows red→amber→green (`viewport[data-match]`), plus the meter bar in the panel. Recognized letter → small corner **badge** instead of a huge centre overlay. (4) **Active correction guide** (`overlay.drawGuide`) — replaces the static ghost: draws YOUR live skeleton with each segment coloured by how far that joint is from the target, plus a "move this way" arrow at every off fingertip. Toggle "correction guide on camera", default on. New shared `js/skeleton.js` (topology + `drawSkeleton` + `vectorToPixels`) used by the live overlay, the guide, and the reference diagram; `overlay.js` no longer depends on MediaPipe `DrawingUtils` and is now synchronous. Self-test 56/56. NOTE: the live *visual* of the guide arrows is unverified in-session (synthetic-camera flakiness in the automation browser) — check on the deployed site.
- **2026-09-03:** Optimization pass (profiled first, then fixed the two real costs). (1) **kNN `classify`: 1.46 ms → 0.37 ms (~4x)** via partial-distance early termination — once k candidates are held, the per-vector distance sum abandons as soon as it exceeds the current k-th best. Exact, not approximate: offline accuracy unchanged at 95.9%. (2) **Freed ~15–20 MB** — the dataset-loader promise was pinning all ~31k expanded row objects for the page's life; it now resolves to a boolean after `createClassifier`/`buildReference` take their own compact copies. Heap settles at ~11 MB after startup. Profiled and left alone (already cheap): `loadDataset` incl. rotation expansion 57 ms, `createClassifier` 3.7 ms, `buildReference` 10 ms, `normalizeLandmarks` 0.007 ms, `drawHands` (MediaPipe DrawingUtils) 0.05 ms. MediaPipe's `detectForVideo` (~15–30 ms) dominates the frame and isn't ours to optimize. Self-test 53/53; live-verified (upright N and 28°-tilted N both → "N 100%").
- **2026-09-03:** M↔N / D↔O tie-breaker — BUILT, TESTED, SHELVED (`js/refine.js`, not wired in). Doesn't help: (1) **M↔N** — tried 13 candidate discriminating measurements, best separation 65% (worse than kNN's own 84% on N). MediaPipe guesses the occluded thumb, so no landmark measurement carries the M-vs-N signal. (2) **D↔O** — the index–thumb-gap measurement IS 90% separable, but kNN never produces thin-margin D-vs-O ties for the rule to catch: the real D errors are D→C/D→P/D→E at unanimous 5-0 votes. Blanket-applying the D/O rule regressed D 87%→84% (it overrides correct calls with its own 10% error). Kept `js/refine.js` as a documented record + ready harness; kept `knn.js`'s new `runnerUp`/`margin` outputs (harmless, useful). **The only real lever left for M 85 / N 84 / D 87 is cleaner data** — self-capture of those specific letters, front-lit, where MediaPipe reads the thumb better. The practice page's ghost + match meter make guided capture straightforward. Self-test 53/53.
- **2026-09-03:** Rotation robustness — SHIPPED. Verified the fix isn't forced upright-alignment (that cratered D −13, H −12, M −11 by erasing angle signal) but **rotation augmentation**: for each real hand the dataset build also stores copies rotated ±15° and ±30° in the image plane (`config.AUGMENT_ROTATIONS`, `normalize.rotateVector()`), so kNN has neighbours at every tilt. Result: **flat ~95% accuracy from −25° to +30° of hand tilt** (was 64% at 25°), overall 95.7%→**95.9%**, no per-letter regressions. Live-verified: a 25°-tilted "N" still reads "N 100%". Efficiency: only the ~6.3k originals are stored (`data/dataset.json` 3.2 MB, down from an 18 MB fully-expanded file); `dataset.js` expands to ~31.6k rows in memory on load via `rotateVector` (~1 ms/query, fine). Coords rounded to 3 decimals (zero accuracy cost — MediaPipe isn't that precise). `tools/test-knn.js` split is now group-aware (rotated copies follow their parent; test set = originals only) so augmentation can't leak. Self-test 48/48. **M 85 / N 84 / D 87 still under 90** — augmentation didn't touch that (it's thumb occlusion), so the M↔N / D↔O tie-breaker is still the next step.
- **2026-09-03:** Pre-tie-breaker audit — robustness-tested the pipeline and researched whether the method is optimal. **Findings:** (1) **Rotation sensitivity is the real weak spot** — a 20° hand tilt drops offline accuracy 95.5%→77%, a 30° tilt →50%. We do no rotation normalization. Tested fix: rotate landmarks in-plane so the wrist→middle-MCP axis points up → **flat 93.4% at any tilt 0–30°** (costs ~2 pts at the ideal angle, worth it). (2) The `radius = max wrist-point distance` scale is fragile — one flew-out fingertip → ~60%; switching to wrist→middle-MCP bone-length scaling recovers ~5 pts on that case. (3) Aspect-ratio handling is fine (invariant under ±33% stretch — earlier worry unfounded); landmark jitter is fine (95%+ at ±0.05); start/stop/flip ×5 leaks nothing. **Method check:** kNN is correct — nearest-centroid (24 vectors, 245× faster) only scored 80–81% because letter classes are multimodal (same letter at different angles = separate clusters); an MLP is the plan's later rung, not needed now; brute-force kNN at 0.29 ms/query needs no k-d tree. **Recommendation (pending user OK):** before the M↔N tie-breaker, upgrade `normalize.js` with in-plane rotation alignment + bone-length scaling and re-extract the dataset (~6 min). Expected to fix live rotation robustness and possibly tighten M/N enough that the tie-breaker isn't needed. Research: MDPI "Improving Hand Pose Recognition Using Localization and Zoom Normalizations"; arXiv 2305.05296.
- **2026-09-03:** UI redesign — practice mode merged into `index.html` (no separate page); layout tightened (whole app now ~370px tall on mobile, fits one screen, was ~2 screens). New: a **"Learn a letter"** chip row (24 letters, scrolls, selected chip auto-centres, right-edge fade hint); picking one shows a **glowing ghost skeleton** of that letter's canonical shape anchored to your live hand — red (off) → amber (close) → green (correct), by similarity to the class centroid AND the classifier agreeing; a **match meter** bar mirrors it; an optional corner **reference photo** (real grassknoted frame). New module `js/reference.js` (UI-free: class centroids + `score()`), `overlay.drawGhost()`, config knobs `MATCH_CLOSE`/`MATCH_CORRECT`/`REFERENCE_IMG`, 24 photos in `assets/reference/`. **Perf bug caught & fixed in testing:** first `drawGhost` used per-shape `shadowBlur` (up to 34px) → would tank real-device fps; rewritten as layered strokes, now 0.009 ms/call (full loop body 0.31 ms — 30 fps cap is nowhere near stressed). Self-test grown to **43 checks, all pass**; live pipeline re-verified via injected hands (exact-N hand → meter 92% green "N 100%"; A hand vs N target → 8% red). Note: `fps` shown in the automation browser is a hidden-tab rAF-throttle artifact, not the app — micro-benchmarked instead. `standalone.html` stays the pre-redesign skeleton-only fallback (not kept in visual sync).
- **2026-09-03:** Full component test pass — **35/35 checks pass**, 0 broken. Saved as a re-runnable harness at `tools/selftest.html` (run after every change). Verified: all 10 JS modules' public APIs; server routes + mime types + 404; `dataset.json` integrity (6321×74, braces balanced, no NaN/null); the live pipeline end to end on `index.html` (synthetic camera + injected fake hand → state reaches `tracking`, a letter is drawn on the overlay, stats badge updates, 0 console errors); `standalone.html`, `tools/extract.html`, `tools/test-knn.html` all load and run clean. Two latent bugs fixed in the pass: (1) `tools/extract.js` (the folder-picker fallback) was still emitting 63-dim, non-canonicalized vectors — would silently corrupt `dataset.json` if used to top up a weak letter; now matches the primary build (74-dim, left→right, correct metadata). (2) removed the now-dead "fold left hands" checkbox from `tools/test-knn.html` (data is canonicalized at build time).
- **2026-09-03:** Accuracy push results — **overall 91.9% → 95.7%** (24 letters, k=5, 20% held-out, 1175 test samples). Rebuilt `data/dataset.json` = 6321 samples, **74-dim vectors** (63 coords + 11 shape features), left hands canonicalized to right at build time. M: 47%→87%, N: 67%→84% (via ~3x more M/N samples + the curl/thumb features). 21 of 24 letters now ≥93%. **Still under target:** M 87%, N 84%, D 87%. Cause identified as a landmark-method ceiling, not tuning: in M/N the thumb is physically occluded behind the folded fingers, so MediaPipe's thumb-position estimate is noisy and the M/N clusters genuinely overlap; D↔O is a near-silhouette lighting issue in grassknoted's D images. k and feature-weight sweeps don't move N/D. Options to close the last gap: (a) an explicit M-vs-N / D-vs-O tie-breaker rule on the single most-discriminative measurement, (b) user self-captures ~40 clean front-lit samples each of M/N/D guided by the new practice-page reference. `js/knn.js` made dimension-agnostic; `js/dataset.js` accepts 63 or 74; `js/main.js` live path now appends the features too.
- **2026-09-03:** Reference-image idea + accuracy push. (1) granthgaurav skeleton dataset confirmed to show **incorrect signs** (its "M" has three fingers extended; real M is a fist) — dropped as a visual reference. Replacement: a new `practice.html` with a letter dropdown showing a correct grassknoted photo + a canonical skeleton generated from our own training-vector centroids, plus a live "match to target" meter and a ghost overlay. Seeds the roadmap's Phase 4 "camera checks your sign." (2) Accuracy work toward 90%+/letter: reprocessing M/N/P/Q/R/C/F at higher volume, and adding ~11 engineered shape features (per-finger curl, adjacent fingertip gaps, thumb-tip position) appended to the raw-63 vector behind a `USE_EXTENDED_FEATURES` flag, so M↔N and R↔U differences become explicit instead of implicit. A/B tested via `tools/test-knn.html`.
- **2026-09-03:** Stage 3 offline accuracy measured — **91.9%** on 24 letters (k=5, 20% held-out, 725 test samples), above the plan's 90% target. 22/24 letters at 89–100%. Only real weak spots: **M 47%, N 67%** (M→N 27%, N→M 25%, M→A 13%). Narrower than the plan's M/N/S/T+A/T watchlist — S and T are 97%. Cause: too few samples (M 76 / N 61) + genuine one-fingertip shape difference. Fix path (deferred to post-Stage-5): reprocess more M/N images from `data/_src/` (only ~200 of 3000 used), then add a finger-angle feature if still weak. Added `config.LETTERS` (24-letter allow-list); `tools/test-knn.js` and live classifier now filter to it.
- **2026-09-03:** DATASET RESOLVED — switched to Kaggle grassknoted/asl-alphabet (the plan's own fallback; real photos, ~3000/letter, the field's benchmark set). Extracted a strided subsample through `HandLandmarker` (IMAGE mode) → `data/dataset.json`: **4023 samples across A–Z + space**, 63-value vectors (with z), 3780 right / 243 left hands. Overall MediaPipe detection rate on this set ≈ 69% (fists self-occlude). **FLAG — weak per-letter sample counts:** N=61, M=76, space=92, C=137 fall below the plan's 150–300 target because MediaPipe fails to find a hand in those handshapes. Enough to build the pipeline end-to-end; if Stage 6 evaluation confirms M/N are weak, top them up by processing more of the 3000 available images (only ~200 were sampled). Raw source images kept in gitignored `data/_src/` for that. `tools/extract.html` (folder-picker version) demoted to a manual fallback; this run used a manifest + browser-driven pass.
- **2026-09-03:** DATASET BLOCKED — the granthgaurav "ASL Mediapipe Landmarked Dataset" turned out to be 400x400 JPG *images of the green skeleton drawn on white* (~180/letter, 4681 total), not numeric landmark vectors. Ran a detection probe: MediaPipe `HandLandmarker` finds 0 hands in these line drawings (it's trained on real hands, not stick figures), so we can't recover coordinates from them. This dataset is unusable for our pipeline. Awaiting decision on replacement — options: (a) grassknoted "ASL Alphabet" real photos + extraction pass (plan's own fallback), (b) a genuinely pre-landmarked CSV dataset, (c) self-capture ~40 samples/letter from webcam. Leaning (c) to unblock Stages 3–5 today, then layer (a) in for weak letters per the existing "scale up selectively" rule.
- **2026-09-03:** Bias/fairness pass — confirmed skin color and hand size are already neutral to the classifier (landmark-based, not pixel-based; size already normalized). Added an explicit skin-tone detection-reliability check to Task 6, since the one residual risk is upstream in MediaPipe's pretrained hand detector, not our classifier. Two-handed neutral tracking approach (mirror each hand individually, then concatenate in a consistent left-then-right order) logged on the project roadmap under Phase 2, since ASL fingerspelling is one-handed and this doesn't block letters.
- **2026-09-03:** Two fixes from a backward-reasoning pass — (1) split the training subsample into train/held-out-test portions (e.g. 80/20) so offline accuracy testing (Task 4) is measured against samples the classifier hasn't seen, not itself; (2) normalize for handedness (mirror left-hand landmarks to match right, or vice versa) before training, since the dataset likely isn't hand-balanced and live signing might use either hand.
- **2026-09-03:** Efficiency change — subsample training data instead of loading the full dataset. kNN doesn't benefit from large N (it only gets slower), so start with ~150–300 samples per letter (stratified) rather than the full pre-annotated set. Raw-image fallback, if triggered, stays small (~50–100 images) and scoped to only the specific underrepresented letter. Scale up data only for whichever letters evaluation (Task 6) actually flags as weak — not as a blanket increase.
- **2026-09-03:** Dataset decided — primary: Kaggle "ASL Mediapipe Landmarked Dataset (A–Z)" (granthgaurav/asl-mediapipe-converted-dataset), landmarks pre-extracted for all 26 letters. Fallback: raw ASL Alphabet dataset (grassknoted, ~87k images) + manual extraction, used only to top up any letter class found underrepresented in the primary set.
- **2026-09-03:** Added J/Z motion-buffer deferral note (§4a) — build/validate the other 24 letters first, add J/Z afterward reusing Task 7's hold/pause detection.
- **2026-09-03:** Incorporated landscape research findings — balance training data per letter (Task 3); widened confusable-letter watchlist from M/N/S to M/N/S/T and A/T (Task 6).
- **2026-09-03:** Initial structured plan created (scope, task breakdown, build order).
